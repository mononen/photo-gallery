Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To access your photo gallery application:

{{- if .Values.ingress.enabled }}
1. The application will be available at:
   {{- range .Values.ingress.hosts }}
   https://{{ .host }}
   {{- end }}

   Note: Please ensure your DNS is configured to point to your ingress controller.
{{- else }}
1. Get the application URL by running:
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "photo-gallery.fullname" . }} 8080:{{ .Values.service.port }}

2. Visit http://127.0.0.1:8080 in your browser
{{- end }}

To check the status of the deployment:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

To view the logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }} -f

